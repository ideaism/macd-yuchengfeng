# The Quantified Self - 数据可视化网页

基于 React + Tailwind CSS + Lucide React Icons 构建的年度数据回顾可视化项目。

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

浏览器会自动打开 `http://localhost:5173`

### 构建生产版本

```bash
npm run build
```

## 📁 项目结构

```
myself/
├── public/
│   └── assets/          # 静态资源目录
│       ├── city/        # 城市背景图片
│       ├── location/    # 地点图片
│       ├── body/        # 身体姿态图片
│       └── music/       # 音乐文件
├── src/
│   ├── components/
│   │   ├── SceneLayers.jsx    # 视觉合成区（3层叠加）
│   │   ├── ControlPanel.jsx   # 数据控制面板
│   │   ├── Calendar.jsx       # 交互日历
│   │   └── MusicWidget.jsx    # 音乐播放器
│   ├── App.jsx               # 主应用组件
│   ├── main.jsx              # 入口文件
│   └── index.css             # 样式文件
└── data.json                 # 数据文件（数组格式）
```

## 🖼️ 图片资源要求

请将图片资源放置在 `public/assets/` 目录下：

```
public/assets/
├── city/
│   ├── wuhan.png
│   ├── winchester.png
│   └── southampton.png
├── location/
│   ├── home_bed.png
│   └── library.png
└── body/
    └── music.png
```

### 图片路径规则

- **City Layer (Z-1)**: `/assets/city/{city}.png`
- **Location Layer (Z-2)**: `/assets/location/{location}.png`
- **Body Layer (Z-3)**: `/assets/body/{status}.png` (自动转为小写)

## 📊 数据格式

`data.json` 文件格式（数组）：

```json
[
  {
    "date": "2025-11-24",
    "time": "night",
    "city": "london",
    "location": "home_bed",
    "status": "Music",
    "secondary": 3,
    "music_src": "/assets/music/jazz.mp3"
  }
]
```

### 字段说明

- `date`: 日期格式 "YYYY-MM-DD"
- `time`: `"sun"` 或 `"night"` - 控制右侧面板背景色和时间图标
- `city`: 城市名称（对应 City Layer 图片）
- `location`: 地点名称（对应 Location Layer 图片）
- `status`: 状态，如 `"Music"`（对应 Body Layer 图片，会自动转为小写）
- `secondary`: 1-10 的数字，用于心情指数条（1-3冷色，4-7中性，8-10暖色）
- `music_src`: 音乐文件路径（可选，当 status 为 "Music" 时使用）

## 🎨 界面布局

### 响应式分屏布局

- **桌面端**:
  - 左侧（65%）：视觉合成区（The Stage）
  - 右侧（35%）：数据控制面板（Control Panel）

- **移动端**:
  - 顶部（60vh）：视觉合成区
  - 底部（40vh）：数据控制面板（可滚动）

### 视觉合成区（The Stage）

包含3层绝对定位的图片层，按 z-index 从低到高：

1. **City Layer (Z-1)**: 城市背景
2. **Location Layer (Z-2)**: 地点/环境
3. **Body Layer (Z-3)**: 人物姿态

### 数据控制面板（Control Panel）

包含以下组件：

1. **标题**: "The Quantified Self"
2. **日期显示**: 格式 "MMM DD, YYYY"
3. **时间状态图标**: 太阳/月亮图标（Lucide Icons）
4. **心情指数条**: 基于 `secondary` 字段的进度条
5. **交互日历**: 月份网格，只有有数据的日期可点击

## ✨ 动画效果

1. **图片切换**: 0.5s 的 Opacity Fade 过渡效果
2. **背景色切换**: 控制面板背景色随 `time` 字段改变（白天浅灰/米色，夜晚深灰/午夜蓝），使用 `transition-colors duration-700`

## 🎵 音乐功能

当 `status` 为 `"Music"` 时：

- 右下角显示音乐播放器组件（MusicWidget）
- 显示跳动的音符动画
- 点击可暂停/播放
- 自动播放（可能被浏览器阻止）

## 🛠️ 技术栈

- **React 18**: UI 框架
- **Vite 5**: 构建工具
- **Tailwind CSS 3**: 样式框架
- **Lucide React**: 图标库

## 📝 开发注意事项

1. 图片路径使用 `/assets/` 前缀
2. 所有图片层必须重叠且尺寸一致
3. 日历中只有 `data.json` 中存在的日期可点击
4. 心情指数条颜色根据 `secondary` 值自动变化
5. 控制面板背景色根据 `time` 字段自动切换

## 🌐 部署

构建后的文件在 `dist` 目录，可直接部署到静态服务器。

```bash
npm run build
# 将 dist 目录内容上传到服务器
```

